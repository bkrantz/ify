#!/usr/bin/env python

import os
import sys
dir_path = os.path.dirname(os.path.realpath(__file__))
sys.path.append(dir_path + '/../src/')
from actions import *


source_file = "/users/bkrantz/catalog/catalog_scanner/css/simple-sidebar.css"
actions = "-m"
target_filename
def process(filename, actions, raw_content, format, tab, output_file, mode, formats):
